# Hello World Script plugin

A simple custom Rundeck script plugin. To install the plugin, run:

```
make install LIBEXT_DIR=~/rundeck/libext
```

Where LIBEXT_DIR is your rundeck installation's libext directory.
